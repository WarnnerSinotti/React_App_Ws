import { DefaultTheme } from "react-native-paper";

export const themeInput = {
    ...DefaultTheme,
    colors:{
        ...DefaultTheme.colors,
        primary: "#999",
        text: '#ffffff', //Texto do Input
        placeholder: '#999',
        
    }
}