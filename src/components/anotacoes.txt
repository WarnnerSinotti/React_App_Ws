import * as React from 'react';
import { View, StyleSheet, StatusBar } from 'react-native';
import { Button, Text, TextInput, Avatar, Divider } from 'react-native-paper';

const Acesso = () => {
  const [Email, setEmail] = React.useState("");
  const [Password, setPassword] = React.useState("");

  return (
    <View style={style.login}>
      <StatusBar style="auto"/>
      <Text>Warnner</Text>
      <Avatar.Icon style={style.avatar} size={50} icon="folder" />
      <TextInput style={style.textInput}
        label="Email"
        mode='outlined'
        value={Email}
        onChangeText={Email => setEmail(Email)}
      />

      <TextInput style={style.textInput}
        label="Senha"
        mode='outlined'
        secureTextEntry = { true }
        right={<TextInput.Icon name="eye" onPress={() => console.log('habilitar')} />}
        value={Password}
        onChangeText={Password => setPassword(Password)}
      />
      <Button style={style.entrar}
        icon="login" mode="contained" onPress={() => console.log('Pressed')}>
        Entrar
      </Button>
      <Divider />
      <Text>Ou</Text>
      <Divider />
      <Button style={style.buttonExterno}
        icon="login" mode="contained" onPress={() => console.log('Pressed')}>
        Facebook
      </Button>
      <Divider />
      <Button style={style.buttonExterno}
        icon="login" mode="contained" onPress={() => console.log('Pressed')}>
        Google
      </Button>
    </View>
  );
};

const style = StyleSheet.create({
  login: {
    backgroundColor: '#666'
  },
  textInput: {
    borderRadius: 40,
    marginLeft: 40,
    marginRight: 40,
    marginBottom: 16
  },
  entrar:{
    padding: 5,
    borderRadius: 40,
    marginLeft: 16,
    marginRight: 16,
    marginBottom: 16
  },
  buttonExterno:{
    padding: 5,
    borderRadius: 40,
    marginLeft: 100,
    marginRight: 100,
    marginBottom: 16,
    backgroundColor: '#666'
  },
  avatar: {
    alignContent: 'center',
    justifyContent: 'center',
    marginHorizontal: '50%',

  }
})

export default Acesso;